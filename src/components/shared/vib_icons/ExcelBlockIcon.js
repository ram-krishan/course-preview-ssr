import React from 'react';
import ExcelIcon from './ExcelIcon';

const ExcelBlockIcon = (props) => (
  <span data-testid="excel-block-icon" {...props}>
    <ExcelIcon />
  </span>
);
export default ExcelBlockIcon;
