import React from 'react';
import PowerPointIcon from './PowerPointIcon';

const PowerPointBlockIcon = (props) => (
  <span data-testid="power-point-block-icon" {...props}>
    <PowerPointIcon />
  </span>
);
export default PowerPointBlockIcon;
